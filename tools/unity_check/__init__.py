"""
Backends for `sf unity check`. Not run directly: the supported entry point is
the `sf` subcommand (PROJECT_PLAN section 8).
`sf unity check` のバックエンド。直接は実行しない。公開の入口は `sf` の
サブコマンドである（PROJECT_PLAN §8）。
"""
